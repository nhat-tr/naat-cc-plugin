#!/usr/bin/env bash
# CLI wrapper for Elasticsearch traffic health report.
# Usage: kibana-traffic prod regrinding
#        kibana-traffic prod calibration --from now-6h
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
exec tsx "$SCRIPT_DIR/../infra/kibana/kibana-traffic.ts" "$@"