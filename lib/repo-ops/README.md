# repo-ops

Shared repo-operations library for the HES multi-repo workspace (bare-repo worktrees, domain groups, per-group Common submodule). Consumed by skills/vuln-autofix and future tools via relative import. Zero deps; run tests with `node --experimental-strip-types --test *.test.ts`.
